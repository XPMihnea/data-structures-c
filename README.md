# Data Structures in C

This project implements fundamental data structures in C:
- stack
- queue

Both are implemented using linked lists and dynamic memory allocation.

## Features

- queue of variable-length strings
- stack of characters
- reversing each word from the queue using a stack

## Compilation

```bash
gcc main.c -o app
